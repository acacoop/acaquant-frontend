import type { NextConfig } from "next";

// Security headers base (sin CSP — el CSP requiere relevar todos los orígenes
// de scripts/fuentes/iframes de TradingView y se deja para una pasada dedicada).
// Estos no rompen funcionalidad: la app es una terminal standalone.
const securityHeaders = [
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "X-Frame-Options", value: "DENY" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=()" },
  { key: "Strict-Transport-Security", value: "max-age=63072000; includeSubDomains" },
];

const nextConfig: NextConfig = {
  async headers() {
    return [{ source: "/:path*", headers: securityHeaders }];
  },
};

export default nextConfig;
